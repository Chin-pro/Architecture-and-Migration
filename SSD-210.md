| 測試類型 | 工具 | 適合測什麼 |
| --- | --- | --- |
| Controller Test | MockMvc | request mapping、status code、validation |
| Integration Test | SpringBootTest + MinIO Testcontainer | Controller + Service + MinIO |
| API-level E2E Test | SpringBootTest RANDOM_PORT + REST Assured + MinIO Testcontainer | 真 HTTP 上傳 / 下載流程 |
| UI E2E Test | Playwright | 前端畫面點擊、上傳、下載 |

下面我直接給你一套**可以落地到 Spring Boot 2 專案的做法**。你可以把它理解成四層測試，但你這次臨時報案最優先做的是：

```
第一優先：Controller Test
目的：確認 API 接口格式、multipart 參數、status code 沒壞。

第二優先：API-level E2E Test
目的：真的啟動 Spring Boot，真的用 HTTP 上傳 / 下載，真的接 MinIO。
```

Spring Boot 官方文件說 `@WebMvcTest` 會自動設定 MockMvc，MockMvc 可以不用啟動完整 HTTP server 就快速測 Controller；而 `@SpringBootTest(webEnvironment = RANDOM_PORT)` 則適合啟動真實 server 測 HTTP 流程。(Home)

MinIO 這種外部服務可以用 Testcontainers 啟動暫時 container，Testcontainers 官方也提供 GenericContainer 與 MinIO container 的使用方式。(Testcontainers for Java)

---

# 0. 你最後要做出的測試分層

我建議你的 test package 長這樣：

```
src/test/java
└── com/company/project
    ├── controller
    │   └── VendorFileControllerTest.java
    │
    ├── integration
    │   ├── support
    │   │   └── MinioTestSupport.java
    │   └── VendorFileIntegrationTest.java
    │
    └── e2e
        └── VendorFileUploadDownloadE2ETest.java

src/test/resources
├── application-e2e.properties
└── e2e
    ├── vendor-upload-valid.xlsx
    ├── vendor-upload-duplicate-name-cy.xlsx
    └── vendor-template.xlsx
```

你的四層測試目的如下：

| 測試類型 | 實作方式 | 是否真的 HTTP | 是否真的 MinIO | 這次建議 |
| --- | --- | --- | --- | --- |
| Controller Test | `@WebMvcTest + MockMvc` | 否 | 否 | 要做 |
| Integration Test | `@SpringBootTest + MinIO Testcontainer` | 可不真 HTTP | 是 | 第二階段 |
| API-level E2E Test | `RANDOM_PORT + REST Assured + MinIO Testcontainer` | 是 | 是 | 要做 |
| UI E2E Test | Playwright | 是 | 透過後端間接碰到 | 暫時不要 |

---

# 1. Maven 設定

你是 Spring Boot 2，所以我不建議一開始用 Testcontainers 2.x 或 Spring Boot 3 才有的新整合方式。先用保守組合：

```xml
<properties>
    <testcontainers.version>1.19.8</testcontainers.version>
</properties>

<dependencyManagement>
    <dependencies>
        <dependency>
            <groupId>org.testcontainers</groupId>
            <artifactId>testcontainers-bom</artifactId>
            <version>${testcontainers.version}</version>
            <type>pom</type>
            <scope>import</scope>
        </dependency>
    </dependencies>
</dependencyManagement>

<dependencies>
    <!-- Spring Boot Test -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-test</artifactId>
        <scope>test</scope>
    </dependency>

    <!-- REST Assured：用來發真實 HTTP request -->
    <dependency>
        <groupId>io.rest-assured</groupId>
        <artifactId>rest-assured</artifactId>
        <scope>test</scope>
    </dependency>

    <!-- Testcontainers core -->
    <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>testcontainers</artifactId>
        <scope>test</scope>
    </dependency>

    <!-- Testcontainers + JUnit 5 -->
    <dependency>
        <groupId>org.testcontainers</groupId>
        <artifactId>junit-jupiter</artifactId>
        <scope>test</scope>
    </dependency>

    <!-- MinIO Java SDK：測試中建立 bucket / 放 template / 驗證 object -->
    <dependency>
        <groupId>io.minio</groupId>
        <artifactId>minio</artifactId>
        <scope>test</scope>
    </dependency>
</dependencies>
```

REST Assured 官方 repo 的測試範例有示範 `multiPart(...)` 搭配 `formParam(...)` 做 multipart upload，所以你的上傳 Excel API 可以用同樣模式測。(GitHub)

---

# 2. 建議加一個 e2e Maven profile

不要讓 E2E Test 進入每次 `mvn test` 必跑。公司內網 CI/CD 未必支援 Docker，所以先用 profile 隔離。

```xml
<profiles>
    <profile>
        <id>e2e</id>
        <build>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-surefire-plugin</artifactId>
                    <configuration>
                        <includes>
                            <include>**/*E2ETest.java</include>
                            <include>**/*IntegrationTest.java</include>
                        </includes>
                    </configuration>
                </plugin>
            </plugins>
        </build>
    </profile>
</profiles>
```

平常開發跑：

```bash
mvn test
```

跑 E2E 時才用：

```bash
mvn test -Pe2e
```

如果公司已經用 `maven-failsafe-plugin` 管 integration test，那更標準是讓 `*IT.java` 或 `*E2ETest.java` 在 `mvn verify -Pe2e` 跑。但臨時報案先不用把 Maven 結構做太大。

---

# 3. `application-e2e.properties`

新增：

```
src/test/resources/application-e2e.properties
```

內容先放非敏感、測試專用設定：

```
# 這些值會被 DynamicPropertySource 動態覆蓋
minio.endpoint=http://localhost:9000
minio.access-key=minioadmin
minio.secret-key=minioadmin
minio.bucket-name=stationery-e2e-test

# 如果專案有 security，可以考慮用測試 profile 關掉或使用測試 token
# app.security.enabled=false
```

重點是：**真正的 MinIO endpoint 不寫死在 properties**。

因為 Testcontainers 啟動 container 後，port 是動態分配的。Spring 的 `@DynamicPropertySource` 正是用來在 integration test 裡註冊這種「測試開始後才知道」的動態屬性，官方文件也明確舉例說這適合 container-managed external resource。(Home)

---

# 4. 你要先確認 production code 讀哪幾個 property

你要去專案裡搜尋：

```java
@Value("${")
```

或：

```java
@ConfigurationProperties
```

你要找出 MinIO 設定實際名稱。

可能是這種：

```java
@Value("${minio.endpoint}")
private String endpoint;

@Value("${minio.access-key}")
private String accessKey;

@Value("${minio.secret-key}")
private String secretKey;

@Value("${minio.bucket-name}")
private String bucketName;
```

也可能是：

```java
@ConfigurationProperties(prefix = "storage.minio")
```

如果是第二種，你測試要塞的 property 就不是：

```java
minio.endpoint
```

而是：

```java
storage.minio.endpoint
storage.minio.access-key
storage.minio.secret-key
storage.minio.bucket-name
```

**這是最容易錯的地方。**

測試明明啟動了 MinIO，但 Spring Boot App 讀到的是空值或舊設定，最後你會誤以為 Testcontainers 壞掉。

---

# 5. 第一層：Controller Test，用 MockMvc 測接口格式

這層不測 MinIO。

它只測：

```
POST /api/vendors/items/upload 是否收 multipart file
siteId 是否必填
沒上傳 file 是否回 400
service 是否被正確呼叫
下載 API 是否回正確 header
```

Spring Boot 官方文件說 `@WebMvcTest` 會自動設定 MockMvc，適合快速測 MVC controller，不需要啟動完整 HTTP server。(Home)

假設你的 Controller 長得像這樣：

```java
@PostMapping("/api/vendors/items/upload")
public ResponseEntity<?> upload(
        @RequestParam("siteId") String siteId,
        @RequestParam("file") MultipartFile file
) {
    vendorFileService.upload(siteId, file);
    return ResponseEntity.ok().body(Map.of("message", "success"));
}
```

那 Controller Test 可以這樣寫：

```java
package com.company.project.controller;

import com.company.project.service.VendorFileService;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.mock.web.MockMultipartFile;
import org.springframework.test.web.servlet.MockMvc;

import static org.mockito.ArgumentMatchers.any;
import static org.mockito.ArgumentMatchers.eq;
import static org.mockito.Mockito.verify;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.multipart;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

@WebMvcTest(VendorFileController.class)
class VendorFileControllerTest {

    @Autowired
    private MockMvc mockMvc;

    @MockBean
    private VendorFileService vendorFileService;

    @Test
    void upload_shouldAcceptMultipartFileAndSiteId() throws Exception {
        MockMultipartFile file = new MockMultipartFile(
                "file",
                "vendor-upload.xlsx",
                "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
                "fake excel content".getBytes()
        );

        mockMvc.perform(
                        multipart("/api/vendors/items/upload")
                                .file(file)
                                .param("siteId", "CY")
                )
                .andExpect(status().isOk());

        verify(vendorFileService).upload(eq("CY"), any());
    }

    @Test
    void upload_shouldReturnBadRequest_whenFileIsMissing() throws Exception {
        mockMvc.perform(
                        multipart("/api/vendors/items/upload")
                                .param("siteId", "CY")
                )
                .andExpect(status().isBadRequest());
    }
}
```

這支測試的價值是：

你以後改 Controller、改參數名稱、改 endpoint，如果不小心破壞前端或 Thunder Client 呼叫格式，這層會先爆。

---

# 6. 建立共用的 MinIO 測試支援類

第二層和第三層都會用到 MinIO，所以建議抽一個測試用 support class：

```
src/test/java/com/company/project/integration/support/MinioTestSupport.java
```

```java
package com.company.project.integration.support;

import io.minio.BucketExistsArgs;
import io.minio.MakeBucketArgs;
import io.minio.MinioClient;
import io.minio.PutObjectArgs;
import org.springframework.core.io.ClassPathResource;
import org.testcontainers.containers.GenericContainer;
import org.testcontainers.containers.wait.strategy.Wait;
import org.testcontainers.utility.DockerImageName;

import java.io.InputStream;

public abstract class MinioTestSupport {

    protected static final String MINIO_USER = "minioadmin";
    protected static final String MINIO_PASSWORD = "minioadmin";
    protected static final String BUCKET_NAME = "stationery-e2e-test";

    protected static final String TEMPLATE_OBJECT_PATH =
            "templates/vendor-upload-template.xlsx";

    protected static final GenericContainer<?> MINIO_CONTAINER =
            new GenericContainer<>(
                    DockerImageName.parse("minio/minio:RELEASE.2023-09-04T19-57-37Z")
            )
                    .withEnv("MINIO_ROOT_USER", MINIO_USER)
                    .withEnv("MINIO_ROOT_PASSWORD", MINIO_PASSWORD)
                    .withCommand("server", "/data")
                    .withExposedPorts(9000)
                    .waitingFor(
                            Wait.forHttp("/minio/health/ready")
                                    .forPort(9000)
                    );

    protected static MinioClient minioClient;

    protected static String getMinioEndpoint() {
        return "http://" + MINIO_CONTAINER.getHost()
                + ":" + MINIO_CONTAINER.getMappedPort(9000);
    }

    protected static void initializeMinio() throws Exception {
        minioClient = MinioClient.builder()
                .endpoint(getMinioEndpoint())
                .credentials(MINIO_USER, MINIO_PASSWORD)
                .build();

        boolean bucketExists = minioClient.bucketExists(
                BucketExistsArgs.builder()
                        .bucket(BUCKET_NAME)
                        .build()
        );

        if (!bucketExists) {
            minioClient.makeBucket(
                    MakeBucketArgs.builder()
                            .bucket(BUCKET_NAME)
                            .build()
            );
        }

        uploadTemplateToMinio();
    }

    private static void uploadTemplateToMinio() throws Exception {
        ClassPathResource templateResource =
                new ClassPathResource("e2e/vendor-template.xlsx");

        try (InputStream inputStream = templateResource.getInputStream()) {
            minioClient.putObject(
                    PutObjectArgs.builder()
                            .bucket(BUCKET_NAME)
                            .object(TEMPLATE_OBJECT_PATH)
                            .stream(inputStream, templateResource.contentLength(), -1)
                            .contentType("application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
                            .build()
            );
        }
    }
}
```

這裡用的是 `GenericContainer`，因為 Testcontainers 官方說 GenericContainer 最有彈性，可以用幾乎任何 container image 當測試依賴。(Testcontainers for Java)

雖然 Testcontainers 有 MinIO module，但在 Spring Boot 2、公司舊專案、臨時報案情境，我會先用 GenericContainer，降低版本衝突風險。MinIO module 官方範例則是 `MinIOContainer` 搭配 MinIO Java client。(Testcontainers for Java)

---

# 7. 第二層：Integration Test，測 Service + MinIO

這層可以不走真實 HTTP。

它測的是：

```
Service 是否能把檔案存到 MinIO
Service 是否能從 MinIO 讀 template
vendor name 重複時是否用 siteId 正確判斷
```

```java
package com.company.project.integration;

import com.company.project.integration.support.MinioTestSupport;
import com.company.project.service.VendorFileService;
import io.minio.StatObjectArgs;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.Tag;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.mock.web.MockMultipartFile;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.context.DynamicPropertyRegistry;
import org.springframework.test.context.DynamicPropertySource;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;

import java.io.InputStream;

import static org.assertj.core.api.Assertions.assertThat;

@Tag("e2e")
@Testcontainers
@ActiveProfiles("e2e")
@SpringBootTest
class VendorFileIntegrationTest extends MinioTestSupport {

    @Autowired
    private VendorFileService vendorFileService;

    @Container
    static final org.testcontainers.containers.GenericContainer<?> minio =
            MINIO_CONTAINER;

    @DynamicPropertySource
    static void registerMinioProperties(DynamicPropertyRegistry registry) {
        registry.add("minio.endpoint", MinioTestSupport::getMinioEndpoint);
        registry.add("minio.access-key", () -> MINIO_USER);
        registry.add("minio.secret-key", () -> MINIO_PASSWORD);
        registry.add("minio.bucket-name", () -> BUCKET_NAME);
    }

    @BeforeAll
    static void beforeAll() throws Exception {
        initializeMinio();
    }

    @Test
    void upload_shouldStoreFileIntoMinio() throws Exception {
        byte[] fileBytes = "fake excel content".getBytes();

        MockMultipartFile file = new MockMultipartFile(
                "file",
                "vendor-upload.xlsx",
                "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet",
                fileBytes
        );

        vendorFileService.upload("CY", file);

        // 這裡 object path 要改成你 service 實際存放的位置
        minioClient.statObject(
                StatObjectArgs.builder()
                        .bucket(BUCKET_NAME)
                        .object("uploads/CY/vendor-upload.xlsx")
                        .build()
        );
    }

    @Test
    void downloadTemplate_shouldReturnBytes() throws Exception {
        byte[] downloadedBytes = vendorFileService.downloadTemplate("CY");

        assertThat(downloadedBytes).isNotEmpty();
    }
}
```

注意：這段的 object path：

```java
"uploads/CY/vendor-upload.xlsx"
```

一定要改成你公司專案實際邏輯。

如果 service 會用 UUID 產生檔名，你就不要硬寫固定 object path，而是從 DB 或 response 拿 object path 再驗證 MinIO。

---

# 8. 第三層：API-level E2E Test，真 HTTP 上傳 / 下載

這層是你這次最有價值的測試。

它會真的啟動 Spring Boot server，真的打 HTTP request，真的接 MinIO。

Spring Boot 官方文件示範 `@SpringBootTest(webEnvironment = WebEnvironment.RANDOM_PORT)` 可以啟動 random port server；文件也有搭配 RestAssured 對 random port 發 request 的範例。(Home)

```java
package com.company.project.e2e;

import com.company.project.integration.support.MinioTestSupport;
import io.minio.StatObjectArgs;
import io.restassured.RestAssured;
import org.junit.jupiter.api.BeforeAll;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.boot.web.server.LocalServerPort;
import org.springframework.core.io.ClassPathResource;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.test.context.DynamicPropertyRegistry;
import org.springframework.test.context.DynamicPropertySource;
import org.testcontainers.junit.jupiter.Container;
import org.testcontainers.junit.jupiter.Testcontainers;

import java.io.File;

import static io.restassured.RestAssured.given;
import static org.assertj.core.api.Assertions.assertThat;
import static org.hamcrest.Matchers.containsString;

@Tag("e2e")
@Testcontainers
@ActiveProfiles("e2e")
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
class VendorFileUploadDownloadE2ETest extends MinioTestSupport {

    @LocalServerPort
    private int port;

    @Container
    static final org.testcontainers.containers.GenericContainer<?> minio =
            MINIO_CONTAINER;

    @DynamicPropertySource
    static void registerMinioProperties(DynamicPropertyRegistry registry) {
        registry.add("minio.endpoint", MinioTestSupport::getMinioEndpoint);
        registry.add("minio.access-key", () -> MINIO_USER);
        registry.add("minio.secret-key", () -> MINIO_PASSWORD);
        registry.add("minio.bucket-name", () -> BUCKET_NAME);
    }

    @BeforeAll
    static void beforeAll() throws Exception {
        initializeMinio();
    }

    @Test
    void uploadVendorItemFile_shouldUploadSuccessfully() throws Exception {
        RestAssured.port = port;

        File uploadFile = new ClassPathResource(
                "e2e/vendor-upload-duplicate-name-cy.xlsx"
        ).getFile();

        given()
                .multiPart(
                        "file",
                        uploadFile,
                        "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
                )
                .formParam("siteId", "CY")
                .when()
                .post("/api/vendors/items/upload")
                .then()
                .statusCode(200)
                .body("message", containsString("success"));

        // 如果你的上傳流程會保存原始檔，可加這段
        // object path 要改成你專案實際儲存邏輯
        minioClient.statObject(
                StatObjectArgs.builder()
                        .bucket(BUCKET_NAME)
                        .object("uploads/CY/vendor-upload-duplicate-name-cy.xlsx")
                        .build()
        );
    }

    @Test
    void downloadVendorTemplate_shouldReturnExcelFile() {
        RestAssured.port = port;

        byte[] downloadedBytes =
                given()
                        .queryParam("siteId", "CY")
                        .when()
                        .get("/api/vendors/items/template")
                        .then()
                        .statusCode(200)
                        .header("Content-Disposition", containsString("attachment"))
                        .extract()
                        .asByteArray();

        assertThat(downloadedBytes).isNotEmpty();
        assertThat(downloadedBytes.length).isGreaterThan(100);
    }
}
```

如果你的 Spring Boot 2 版本比較新，`@LocalServerPort` 可能要改成：

```java
import org.springframework.boot.test.web.server.LocalServerPort;
```

如果比較舊，通常是：

```java
import org.springframework.boot.web.server.LocalServerPort;
```

以 IDE 能 compile 的為準。

---

# 9. 如果下載 API 回傳 presigned URL

有些系統不會直接回傳檔案，而是回傳：

```json
{
  "downloadUrl": "http://..."
}
```

那 E2E Test 要變成兩段：

```java
@Test
void downloadVendorTemplate_shouldReturnUsablePresignedUrl() {
    RestAssured.port = port;

    String downloadUrl =
            given()
                    .queryParam("siteId", "CY")
                    .when()
                    .get("/api/vendors/items/template-url")
                    .then()
                    .statusCode(200)
                    .extract()
                    .path("downloadUrl");

    byte[] downloadedBytes =
            given()
                    .when()
                    .get(downloadUrl)
                    .then()
                    .statusCode(200)
                    .extract()
                    .asByteArray();

    assertThat(downloadedBytes).isNotEmpty();
}
```

不要只驗證 `downloadUrl` 不為空。

因為 URL 有值，不代表真的能下載。

---

# 10. 第四層：UI E2E Test，用 Playwright

這層暫時不是你這次臨時報案的第一優先。

它測的是：

```
使用者真的在瀏覽器點上傳
選檔
按送出
畫面出現成功
點下載
下載到檔案
```

如果公司前端是 React / Vue，Playwright 通常放在前端 repo：

```
e2e
└── vendor-file.spec.ts
```

概念會像這樣：

```tsx
import { test, expect } from '@playwright/test';

test('vendor item upload and template download', async ({ page }) => {
  await page.goto('/vendor/items');

  await page.selectOption('[data-testid="site-select"]', 'CY');

  await page.setInputFiles(
    '[data-testid="vendor-upload-input"]',
    'tests/fixtures/vendor-upload-duplicate-name-cy.xlsx'
  );

  await page.click('[data-testid="upload-button"]');

  await expect(page.getByText('success')).toBeVisible();

  const downloadPromise = page.waitForEvent('download');
  await page.click('[data-testid="download-template-button"]');
  const download = await downloadPromise;

  expect(download.suggestedFilename()).toContain('.xlsx');
});
```

但這層有一個問題：

它需要前端、後端、DB、MinIO 都準備好。對臨時 bug fix 來說成本太高。你可以先不做。

---

# 11. 這次 ticket 我建議你真正提交哪幾支測試

不要一次塞太多，容易被 reviewer 擋。

我建議第一版 PR 放這三支：

```
1. VendorFileControllerTest
目的：確認 multipart file + siteId 的 API 格式。

2. VendorFileUploadDownloadE2ETest
目的：真 HTTP 上傳 Excel，真 MinIO，驗證下載 template。

3. VendorFileDuplicateVendorNameE2ETest
目的：嘉義廠區與台南廠區 vendor name 重複時，使用 siteId 不會抓錯 vendor。
```

第三支可以跟第二支合併，如果你不想測試檔太多。

---

# 12. 針對你這次 bug 的測試情境應該這樣設計

你的 bug 核心不是 MinIO。

你的 bug 核心是：

```
嘉義廠區新增後，
vendor name 與台南廠區重複，
原本 getMapName 只靠 vendor name 對應，
導致上傳 / 刪除時抓錯 vendor。
```

所以 E2E Test 的資料應該刻意準備這種狀況：

```
site table:
Tainan siteId = TN
Chiayi siteId = CY

vendor table:
site_id = TN, vendor_name = ABC供應商, vendor_code = TN001
site_id = CY, vendor_name = ABC供應商, vendor_code = CY001
```

測試行為：

```
Given:
  DB 裡有兩個相同 vendor_name，但 site_id 不同

When:
  使用 siteId = CY 上傳 Excel
  Excel 裡 vendor_name = ABC供應商

Then:
  系統應該對應到 CY 的 vendor
  不可以對應到 TN 的 vendor
```

如果你的 API response 會回 vendorId 或 vendorCode，直接驗證：

```java
.body("vendorCode", equalTo("CY001"))
```

如果 response 不回，就查 DB：

```java
@Autowired
private JdbcTemplate jdbcTemplate;

@Test
void upload_shouldMapVendorBySiteIdAndVendorName() throws Exception {
    RestAssured.port = port;

    File uploadFile = new ClassPathResource(
            "e2e/vendor-upload-duplicate-name-cy.xlsx"
    ).getFile();

    given()
            .multiPart("file", uploadFile,
                    "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet")
            .formParam("siteId", "CY")
            .when()
            .post("/api/vendors/items/upload")
            .then()
            .statusCode(200);

    String actualVendorCode = jdbcTemplate.queryForObject(
            "SELECT vendor_code FROM uploaded_vendor_items WHERE site_id = ?",
            String.class,
            "CY"
    );

    assertThat(actualVendorCode).isEqualTo("CY001");
}
```

SQL table 名稱要改成你公司實際的 table。

如果你不能改 DB 結構，但可以在測試資料中 insert vendor/site，這樣最適合。

---

# 13. 測試資料初始化怎麼做？

你有三種選擇。

## 做法 A：用 `@Sql`

適合臨時報案，最直覺。

```java
@Sql(scripts = "/sql/vendor-site-test-data.sql")
@Test
void upload_shouldMapVendorBySiteIdAndVendorName() {
    // test body
}
```

新增：

```
src/test/resources/sql/vendor-site-test-data.sql
```

內容大概：

```sql
DELETE FROM vendor_items;
DELETE FROM vendor;
DELETE FROM site;

INSERT INTO site (
    site_id,
    site_name,
    site_display_name
) VALUES
    ('TN', 'Tainan', '台南廠區'),
    ('CY', 'Chiayi', '嘉義廠區');

INSERT INTO vendor (
    site_id,
    vendor_code,
    vendor_name,
    contact_name,
    phone,
    fax,
    email
) VALUES
    ('TN', 'TN001', 'ABC供應商', '王先生', '000', '000', 'tn@example.com'),
    ('CY', 'CY001', 'ABC供應商', '李先生', '000', '000', 'cy@example.com');
```

## 做法 B：用 `JdbcTemplate` 在 `@BeforeEach` 塞資料

適合你想用 Java 控制資料。

```java
@BeforeEach
void setUpData() {
    jdbcTemplate.update("DELETE FROM vendor_items");
    jdbcTemplate.update("DELETE FROM vendor");
    jdbcTemplate.update("DELETE FROM site");

    jdbcTemplate.update(
            "INSERT INTO site(site_id, site_name, site_display_name) VALUES (?, ?, ?)",
            "TN", "Tainan", "台南廠區"
    );

    jdbcTemplate.update(
            "INSERT INTO site(site_id, site_name, site_display_name) VALUES (?, ?, ?)",
            "CY", "Chiayi", "嘉義廠區"
    );

    jdbcTemplate.update(
            "INSERT INTO vendor(site_id, vendor_code, vendor_name) VALUES (?, ?, ?)",
            "TN", "TN001", "ABC供應商"
    );

    jdbcTemplate.update(
            "INSERT INTO vendor(site_id, vendor_code, vendor_name) VALUES (?, ?, ?)",
            "CY", "CY001", "ABC供應商"
    );
}
```

## 做法 C：用既有測試資料

如果公司專案已經有 test seed data，就不要自己重造。

直接沿用既有資料，但這比較容易被資料變動影響。

---

# 14. CI/CD 中怎麼放

我建議你在 PR 裡先這樣處理：

```java
@Tag("e2e")
class VendorFileUploadDownloadE2ETest {
}
```

本機跑：

```bash
mvn test -Pe2e
```

CI/CD 暫時不要預設跑。

你可以跟 Mentor 說：

```
這支測試會啟動 MinIO container，因此 CI runner 需要支援 Docker。
第一版我先用 @Tag("e2e") 與 Maven profile 隔離，不放進預設 mvn test。
等確認內網 CI/CD 是否允許 Docker socket / DinD / 公司內部 image registry 後，再接到 nightly 或 release pipeline。
```

這樣比較穩。

---

# 15. 最後你在 PR 描述可以這樣寫

```
本次修正新增上傳 / 下載流程測試，分成兩層：

1. Controller Test
   - 使用 MockMvc 驗證 multipart upload API 的 request mapping、必要參數與 status code。
   - 不依賴 MinIO，確保測試快速且穩定。

2. API-level E2E Test
   - 使用 SpringBootTest RANDOM_PORT 啟動真實 HTTP server。
   - 使用 REST Assured 發送 multipart upload request。
   - 使用 Testcontainers 啟動獨立 MinIO container。
   - 測試開始前建立測試 bucket 並放入 template。
   - 驗證上傳 Excel 與下載 template 流程。
   - 針對嘉義 / 台南 vendor name 重複情境，確認系統使用 siteId 正確對應 vendor。

E2E Test 已使用 e2e profile / tag 隔離，不會進入預設 unit test 流程，避免影響目前內網 CI/CD。
```

---

# 16. 你現在的實作順序

照這個順序做，不要跳：

```
1. 找出專案 MinIO property 名稱
2. 新增 application-e2e.properties
3. 新增 test resources 裡的 Excel template / upload file
4. 新增 Controller Test
5. 新增 MinioTestSupport
6. 新增 API-level E2E Test
7. 本機確認 Docker 可以跑
8. 本機執行 mvn test -Pe2e
9. 再跟 Mentor / DevOps 討論要不要進 CI/CD
```

你現在最小可交付版本其實只需要：

```
VendorFileControllerTest
VendorFileUploadDownloadE2ETest
MinioTestSupport
application-e2e.properties
測試用 xlsx files
```

這樣就已經能把「上傳 / 下載檔案」納入代碼測試，而且不會一開始就衝擊公司內網 CI/CD。
